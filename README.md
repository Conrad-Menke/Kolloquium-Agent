# Kolloqium-Agent

Agent-friendly repository for Kolloquium (seminar / conference / colloquium) workflows.

## Purpose

Placeholder — describe what this agent does here.

## Agent setup

This repo is configured for opencode agents. See `AGENTS.md` for agent-specific
instructions and `opencode.json` for tool configuration.

## Structure

```
.
├── AGENTS.md       # Agent instructions (read first)
├── opencode.json   # opencode configuration
├── .gitignore
└── README.md
```
